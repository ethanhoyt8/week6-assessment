const shuffle = require("../src/shuffle");


describe("shuffle should...", () => {
  test('shuffle should return an array', () => {
    let shuffleArray1 = shuffle(testData.shuffle)
    expect(typeof shuffleArray1).toBe('array')
  })

  test('shuffle should move items around', () => {
    
  })
  })

